# PWA V2.0 升级说明

## 🎉 V2.0 主要变化

### ✅ 新功能
1. **云端同步** - 基于Supabase，多设备自动同步
2. **今日任务清单** - 智能计算每日需要采集的患者
3. **按患者管理** - 界面重构为患者视角，不是时间节点视角
4. **日期自动计算** - 根据手术日期自动提醒POD1/3/7/14/30
5. **优先级分级** - 🔴紧急 / 🟡即将到期 / ✅已完成

### 🎨 设计升级
- 温暖的奶油色系（#F7F4EF背景，#C4612F强调色）
- 编辑衬线字体（Playfair Display）
- 毛玻璃效果的粘性导航栏
- 圆角胶囊按钮 + 悬浮动效

### 🔧 技术架构变化
| 项目 | V1.0 | V2.0 |
|------|------|------|
| **数据存储** | LocalStorage | Supabase云端数据库 |
| **用户认证** | 无 | 匿名登录（可扩展邮箱登录）|
| **主页面** | 患者列表 | 今日任务清单 |
| **导航结构** | 时间节点切换 | 患者详情页内切换 |
| **同步方式** | 手动备份JSON | 自动实时同步 |

---

## 📂 文件对照表

| 文件 | 用途 | 变化 |
|------|------|------|
| **index.html** | 主页面 | ✏️ 样式大幅更新（新色系+字体） |
| **app.js** → **app-v2.js** | 应用逻辑 | 🆕 完全重写（云端+任务系统） |
| **database.js** | 🆕 新增 | 封装Supabase操作 |
| **config.js** | 🆕 新增 | Supabase配置（需填写密钥）|
| **supabase_schema.sql** | 🆕 新增 | 数据库表结构 |
| **DEPLOYMENT_GUIDE.md** | 🆕 新增 | 部署步骤详解 |
| **service-worker.js** | 离线支持 | ✏️ 缓存文件列表更新 |
| **manifest.json** | PWA配置 | ✏️ 主题色更新 |

---

## 🚀 如何部署V2.0

### 第一步：配置Supabase（必须）

1. **注册并创建项目**
   - 访问 https://supabase.com
   - 创建新项目（2分钟初始化）

2. **执行数据库脚本**
   - Supabase后台 → SQL Editor
   - 复制 `supabase_schema.sql` 全部内容
   - 粘贴并点击 **RUN**

3. **获取API密钥**
   - Project Settings → API
   - 复制 **Project URL** 和 **anon public key**

4. **填写配置文件**
   - 编辑 `config.js`：
   ```javascript
   const SUPABASE_CONFIG = {
       url: 'https://你的项目ID.supabase.co',  // ← 改这里
       anonKey: '你的anon-key'  // ← 改这里
   };
   ```

### 第二步：本地测试

```powershell
cd pwa_app
python -m http.server 8000
```

手机浏览器访问 `http://你的电脑IP:8000`（电脑和手机同一WiFi）

### 第三步：部署到云端（推荐Vercel）

```powershell
npm install -g vercel
cd pwa_app
vercel
```

按提示操作，获得一个 `https://xxx.vercel.app` 网址。

**详细步骤参考 [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)**

---

## ⚠️ 重要提示

### V1.0数据迁移
**V1.0的LocalStorage数据不会自动迁移到V2.0！**

如果你已经在V1.0中录入了数据：
1. V1.0界面 → "备份数据到本地" → 下载JSON文件
2. 部署V2.0后，需要手动重新录入（或我帮你写个迁移脚本）

### 文件替换清单
在 `index.html` 中，需要把：
```html
<script src="app.js"></script>
```
改为：
```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="config.js"></script>
<script src="database.js"></script>
<script src="app-v2.js"></script>
```

**我已经在新版 index.html 中改好了，直接使用即可。**

---

## 🧪 测试清单

部署完成后，请测试以下功能：

- [ ] 首次打开自动创建匿名账户
- [ ] 添加患者（含入组日期、手术日期）
- [ ] 今日任务清单显示正确（红色/黄色分级）
- [ ] 点击患者进入详情页
- [ ] 填写T0数据并保存
- [ ] 刷新页面，数据依然存在
- [ ] 换设备登录（同一匿名账户ID），数据不同步（预期行为，除非实现邮箱登录）

---

## 📊 功能对比

| 功能 | V1.0 | V2.0 |
|------|------|------|
| 离线使用 | ✅ | ✅ |
| 多设备同步 | ❌ | ✅（同账号）|
| 今日任务提醒 | ❌ | ✅ |
| 按患者管理 | ❌ | ✅ |
| 日期自动计算 | ❌ | ✅ |
| 数据备份 | 手动JSON | 自动云端 |
| Excel导出 | ❌ | 🚧 待开发 |
| 完整字段集 | 30% | 30%（待扩展）|

---

## 🛠️ 下一步开发（V3.0规划）

1. **邮箱登录**（替代匿名账户，真正实现跨设备同步）
2. **Excel导出**（对齐V3统计表格式）
3. **完整字段集**（补充剩余70%字段）
4. **推送提醒**（每天早上8点推送今日任务）
5. **患者搜索/筛选**
6. **数据统计预览**（已完成率、POCD阳性率等）

---

## ❓ 需要帮助？

- **配置问题**：检查 `config.js` 中的URL和Key是否正确
- **连接失败**：打开浏览器F12 → Console，看错误信息
- **数据不同步**：确认已登录（Console会显示User ID）
- **想要V1→V2迁移脚本**：告诉我，我帮你写

**V2.0已完成开发，准备好就按上面步骤部署测试！🚀**
